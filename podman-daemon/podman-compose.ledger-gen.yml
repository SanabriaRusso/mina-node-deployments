version: '3.8'
services:
  mina-daemon:
    volumes:
      - ~/github/mina:/app/:rw
    environment:
      - GENESIS_TIMESTAMP=${GENESIS_TIMESTAMP}
      - MINA_PRIVKEY_PASS=${MINA_PRIVKEY_PASS}
    entrypoint:
      - /usr/bin/env
      - bash
      - -c
      - |
        chmod +x /app/scripts/generate-ledger-hf-dryrun.sh
        /app/scripts/generate-ledger-hf-dryrun.sh -p 5 \
                                                  -k 100 \
                                                  -e 0 \
                                                  -b 100000 \
                                                  --plain-balance 1000000 \
                                                  --per-key-password \
                                                  --mina-binary $(which mina) \
                                                  --runtime-genesis-ledger-binary $(which mina-create-genesis) \
                                                  --output-dir /output \
                                                  --pad-app-state \
                                                  --timestamp ${GENESIS_TIMESTAMP} \
                                                  --prefix ${LEDGER_PREFIX:-new-devnet}
